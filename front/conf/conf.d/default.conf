upstream Backend {
    server back-end;
}

server {
    listen 80;

    root /usr/share/nginx/html

    index index.html index.htm index.nginx-debian.html

    server_name _;

    location / {
        proxy_pass http://Backend;
    }
}