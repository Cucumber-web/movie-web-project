upstream Backend {
    server back-end;
}

server {
    listen 80;

    location / {

        root /usr/share/nginx/html

        index index.html index.htm index.nginx-debian.html

    }

    location /api{
        proxy_pass http://Backend;
    }
}